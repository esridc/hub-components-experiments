# Sends an MS Teams notification if a PR is marked Stale
# Currently uses MessageCard and not AdaptiveCard.
# Changing to AdaptiveCard might require forking & changing the action

name: Notify Team on stale pull requests

on: 
  issues:
    types: [labeled]
    # Use issues here b/c pull_request is only sent when merge doesn't have conflicts
    # and we want to send notifications of all Stale PR, even if there is a merge conflict
  
jobs:
  notify:
    name: Send MS notifications for Stale PR/Issue
    if: ${{ github.event.label.name == 'stale' }}
    runs-on: ubuntu-latest
    steps:
      - name: Debug GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"         
      - uses: actions/checkout@v2
      - name: Notify dedicated teams channel
        uses: skitionek/notify-microsoft-teams@master
        with:
          webhook_url: ${{ secrets.MS_TEAMS_HUB_NOTIFICATIONS_WEBHOOK_URI }}
          # for possible github properties see
          # https://docs.github.com/en/developers/webhooks-and-events/webhooks/webhook-events-and-payloads
          raw: >-
            {
                "@type": "MessageCard",
                "@context": "http://schema.org/extensions",            
                "themeColor": "0076D7",
                "summary": "Stale Pull Request",
                "sections": [{
                    "activityTitle": "**Stale Pull Request / Issue**",
                    "activitySubtitle": "${{ github.event.issue.title }} from ${{ github.event.issue.user.login }}",
                    "activityImage": "${{ github.event.issue.user.avatar_url }}",
                    "facts": [{
                        "name": "Repository",
                        "value": "${{ github.event.pull_request.head.repo.full_name }}"
                    },{
                        "name": "Requested Reviewers",
                        "value": "${{ join(github.event.issue.assignees.*.login, ', ') }}"
                    }, {
                        "name": "Labels",
                        "value": "${{ join(github.event.issue.labels.*.name, ', ') }}"
                    }, {
                        "name": "Updated",
                        "value": "${{ github.event.issue.updated_at }}"
                    }, {
                        "name": "Status",
                        "value": "${{ github.event.issue.state }}"
                    }],
                    "markdown": true
                }],
                "potentialAction": [{
                    "@type": "OpenUri",
                    "name": "View the Issue",
                    "targets": [{
                        "os": "default",
                        "uri": "${{ github.event.issue.html_url }}"
                    }]
                }]
            }
